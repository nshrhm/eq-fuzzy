# Shared core

Reserved for reusable EQ-Fuzzy implementation code.

No large extraction is part of the compatibility-first migration. Move code here only after SCIS or ICICIC actually needs it, and keep `src/iceccme2026/` compatibility imports working.
