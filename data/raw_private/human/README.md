# Private Human Input

Place the original SurveyMonkey export workbook here.

Do not commit the raw export.
The export may contain metadata fields that should remain private even if the questionnaire itself was designed to avoid collecting personal information.
